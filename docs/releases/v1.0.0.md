# Release v1.0.0

[![GitHub Downloads](https://img.shields.io/github/downloads/alexdelprete/ha-droplet-plus/v1.0.0/total?style=for-the-badge)](https://github.com/alexdelprete/ha-droplet-plus/releases/tag/v1.0.0)

**Release Date:** 2026-08-16

**Type:** Major release - First stable release of the Droplet Plus integration.

## What's Changed

This is the first official release, covering the complete feature set of the
integration plus all improvements since the beta.

### Added

- Full Droplet integration with pydroplet accumulators
- Automatic device discovery via Zeroconf
- Real-time water flow rate and volume monitoring
- Consumption tracking (hourly, daily, weekly, monthly, yearly, lifetime)
- Water cost estimation with configurable tariff
- Flow statistics (averages, peaks, minimums over various periods)
- Leak detection with configurable threshold
- Device triggers for leak events
- Diagnostics support
- Config flow with manual and Zeroconf setup
- Options flow for tariff and leak threshold configuration

### Changed

- Minimum supported Home Assistant version raised to **2026.3.0**
  (first HA release on the Python 3.14 baseline)
- pydroplet requirement bumped to `~=2.4.0`

### Fixed

- Flow rate sensors now default to **gal/min** on imperial (US customary)
  installs, matching the volume sensors shown in gallons. Metric installs are
  unaffected, and manually chosen units are preserved. Reported in #12 —
  thanks to guytpetj ([@guytpetj](https://github.com/guytpetj))!

> **Upgrade note for existing installs:** Home Assistant stores a sensor's
> suggested unit in the entity registry the first time the entity is created
> and never updates it afterwards. If you upgraded from the beta, your
> existing flow sensors will keep showing L/min. To switch them to gal/min,
> either set the unit manually per entity (entity settings → *Unit of
> measurement*) or remove and re-add the integration (historical data for
> re-created entities is preserved as long as entity IDs stay the same).

### Quality

- Test coverage raised to **100%** (113 tests)
- Type checking (ty) added to the lint pipeline

**Full Changelog**:
[compare/v0.1.0-beta.1...v1.0.0](https://github.com/alexdelprete/ha-droplet-plus/compare/v0.1.0-beta.1...v1.0.0)
